/* export const EnvConfiguration = () => ({
  environment: process.env.NODE_ENV || 'dev',
  mongodb: process.env.MONGO_URL,
  port: process.env.PORT, // No se puede utilizar en el main.ts
  default_limit: +process.env.DEFAULT_LIMIT || 10,
});
 */
